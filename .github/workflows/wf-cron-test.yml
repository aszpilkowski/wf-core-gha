name: WF Cron Test Reusable Workflow

on: workflow_dispatch

jobs:
  cron_job_main:
    runs-on: ubuntu-latest
    if: ${{ github.ref }} == 'refs/heads/main' || ${{ github.ref }} == 'refs/heads/wf_26'
    
    steps:
      - name: echo ref
        run: echo ${{ github.ref }}
      - uses: .github/workflows/wf-core-main-action.yml@main
          
  cron_job_wf_26:
    runs-on: ubuntu-latest

    steps:
      - uses: .github/workflows/wf-core-main-action.yml@wf_26

  cron_job_wf_31:
    runs-on: ubuntu-latest

    steps:
      - uses: .github/workflows/wf-core-main-action.yml@wf_31
        
      # Add your job steps here
